type ItemType = {
    title: string;
    dataIndex: string;
}

export type { ItemType }

type DataType = {
    key: string | number;
    name: string;
    start: string;
    end: string;
    kind: string;
}

export type { DataType }